<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Phosphor.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Phosphor"
    Title="Phosphor">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom title bar -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="48" Background="Transparent">
            <StackPanel Orientation="Horizontal" Spacing="12" Margin="16,0">
                <TextBlock Text="&#x2B21;" Foreground="{StaticResource PhosphorGreenBrush}"
                           FontSize="18" VerticalAlignment="Center" />
                <TextBlock Text="Phosphor" FontWeight="SemiBold" FontSize="13"
                           VerticalAlignment="Center" />
            </StackPanel>
        </Grid>

        <!-- Navigation shell (visible when authenticated) -->
        <NavigationView
            x:Name="NavView"
            Grid.Row="1"
            IsBackButtonVisible="Auto"
            IsBackEnabled="False"
            PaneDisplayMode="LeftCompact"
            BackRequested="NavView_BackRequested"
            SelectionChanged="NavView_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Sessions" Tag="sessions">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="List" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="Settings" Tag="settings">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Setting" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Frame x:Name="NavContentFrame" />
        </NavigationView>

        <!-- Login frame (visible when not authenticated, sits on top of NavView) -->
        <Frame x:Name="LoginFrame" Grid.Row="1" />
    </Grid>
</Window>
